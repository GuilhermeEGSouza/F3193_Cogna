# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: zcogna.ewm.packoutbdlvs1
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        ignoreCertError: true
        target:
          url: https://vhediwddwd01.cogna.com.br:44380
          client: '100'
        app:
          name: zcogpackdlv
          package: ZEWM
          transport: S4DK974145
        exclude:
          - /test/
